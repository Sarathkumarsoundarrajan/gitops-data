apiVersion: v1
kind: Service
metadata:
  labels:
    app: ideajumble-portal-react
  name: ideajumble-portal-react-svc
  namespace: tt2024-dev
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: ideajumble-portal-react

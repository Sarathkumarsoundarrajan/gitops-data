apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-sbioa-mongodb
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 2Gi
  volumeName: pv-sbioa-mongodb

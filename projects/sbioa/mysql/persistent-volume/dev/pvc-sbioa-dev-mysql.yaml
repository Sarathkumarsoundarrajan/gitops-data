apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-sbioa-dev-mysql
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 2Gi
  volumeName: pv-sbioa-dev-mysql

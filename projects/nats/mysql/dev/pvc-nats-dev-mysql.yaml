apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nats-dev-mysql
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 2Gi
  volumeName: pv-nats-dev-mysql